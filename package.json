{
  "name": "lenovobeast-profile",
  "version": "1.0.0",
  "homepage": "https://lenovobeast.github.io/Playground/",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "preview": "vite preview",
    "capture:preview": "node scripts/capture-preview.mjs",
    "encode:preview": "bash scripts/encode-preview.sh",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "playwright": "^1.62.1",
    "vite": "^6.4.3"
  },
  "dependencies": {
    "three": "^0.168.0"
  }
}
